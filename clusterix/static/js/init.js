$(function() {
    hljs.initHighlightingOnLoad();

    // Inputs
    DataInput.init();

    // Views
    Panels.init();

    // Router & Validation
    Router.init();

    // Search
    Search.init();
});